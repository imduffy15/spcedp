# Security

Report vulnerabilities privately through GitHub's **Security → Report a
vulnerability** page. Fixes target the current version.

EDP v2 uses plaintext or AES-128-ECB without message authentication. Keep the
receiver on a trusted network or use an authenticated tunnel. Encryption
alone does not prevent tampering or replay.
